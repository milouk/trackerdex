# Copy to .env.local and fill in your Pi-hole's URL.
# When set, `npm run dev` proxies /api/* to this host so the browser
# treats Pi-hole as same-origin (no CORS, no mixed-content errors).
VITE_PIHOLE_URL=http://pihole.lan
